name: Legacy source-lead collector (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the current workflow
        run: |
          echo "This legacy workflow is intentionally disabled."
          echo "Use the Google Sheet queue and local Danasus Codex automation instead."
          exit 1
