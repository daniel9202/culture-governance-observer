name: Validate data
on:
  pull_request:
    paths:
      - "data/input/**"
      - "scripts/build_data.py"
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/build_data.py
