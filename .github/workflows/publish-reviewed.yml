name: Legacy Google Sheet publisher (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the current workflow
        run: |
          echo "This service-account fallback is intentionally disabled."
          echo "Use the local Danasus Codex publishing flow after human approval."
          exit 1
